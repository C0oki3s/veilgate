version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: veilgate
    main: ./cmd/veilgate
    binary: veilgate
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{.Version}}
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
  - id: veilgate-admin
    main: ./cmd/admin
    binary: veilgate-admin
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - id: veilgate
    ids:
      - veilgate
    name_template: "veilgate_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats:
      - tar.gz
    files:
      - configs/veilgate.yaml
  - id: veilgate-admin
    ids:
      - veilgate-admin
    name_template: "veilgate-admin_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    formats:
      - tar.gz

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

release:
  github:
    owner: C0oki3s
    name: veilgate
  draft: false
  prerelease: auto
  name_template: "v{{.Version}}"

changelog:
  use: github
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - Merge pull request
      - Merge branch
